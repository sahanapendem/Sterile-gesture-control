Touchless innovation for a safer, smarter healthcare environment
# 1. Clone the repository
git clone https://github.com/sahanapendem/Sterile-gesture-control.git

# 2. Move into directory
cd Sterile-gesture-control

# 3. Install dependencies
pip install flask

# 4. Run the app
python app.py
